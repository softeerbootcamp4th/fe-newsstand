{
  "packageManager": "yarn@4.3.1",
  "type": "module",
  "scripts": {
    "start": "concurrently \"live-server src --mount=/src:./src\" \"json-server --watch src/data.json --port 3001\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^1.0.0-beta.1",
    "live-server": "^1.2.2"
  }
}
